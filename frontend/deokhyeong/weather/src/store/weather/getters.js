const currentWeather = (state) => state.currentWeather
const currentAirPollution = (state) => state.currentAirPollution
const oneWeekWeathers = (state) => state.oneWeekWeathers
const hourlyWeathers = (state) => state.hourlyWeathers

export default {
  currentWeather,
  currentAirPollution,
  oneWeekWeathers,
  hourlyWeathers,
}

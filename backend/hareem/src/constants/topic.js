const MESSAGE_TYPE = {
  LOGGING: 'logging',
  TRACKING: 'tracking',
};

const DATASET_NAME = 'internship-hareem';

const DATA_NAME = {
  USERS: 'users',
  BOOKS: 'books',
  RENTALS: 'rentals',
  RESERVATIONS: 'reservations',
};

module.exports = {
  MESSAGE_TYPE,
  DATASET_NAME,
  DATA_NAME,
};

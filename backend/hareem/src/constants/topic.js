const MESSAGE_TYPE = {
  EVENT: 'event',
};

const DATASET_NAME = 'internship-hareem';

const DATA_NAME = {
  RENTALS: 'rentals',
};

module.exports = {
  MESSAGE_TYPE,
  DATASET_NAME,
  DATA_NAME,
};

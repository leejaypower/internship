type auth {
    next: String
}

type Query {
    isAuthenticatedUser(role: String): auth
    isAuthenticatedAdmin(role: String): auth
}

type Mutation {
    isAuthenticatedUser(role: String): auth
    isAuthenticatedAdmin(role: String): auth
}
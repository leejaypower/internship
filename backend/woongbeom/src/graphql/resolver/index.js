const Book = require('./book');
const User = require('./user');
const admin = require('./admin');
const Auth = require('./auth');
const Rental = require('./rental');

module.exports = {
  Book, User, admin, Auth, Rental,
};
